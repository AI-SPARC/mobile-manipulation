type: package

builders:
  - type: doxygen
    name: html_docs
    source_dir: src
    doxyfile: Doxyfile
    no_javasphinx: true
    # Tente forçar o rosdoc2 a não usar defaults que limpam a saída
    rosdoc2_settings:
      always_run_doxygen: true